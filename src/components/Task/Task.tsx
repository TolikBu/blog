import { FC } from "react";
import classes from "./Task.module.sass";

const Task: FC = () => {
  return (
    <div className={classes["task-container"]}>
      <div></div>
    </div>
  );
};

export default Task;
